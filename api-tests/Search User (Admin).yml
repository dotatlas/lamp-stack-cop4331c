info:
  name: Search User (Admin)
  type: http
  seq: 9

http:
  method: GET
  url: "{{urlBase}}/api/index.php?admin=users&q=Gabriel"
  headers:
    - name: X-User-Id
      value: "5"
  params:
    - name: admin
      value: users
      type: query
    - name: q
      value: Gabriel
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
