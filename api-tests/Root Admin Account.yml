info:
  name: Root Admin Account
  type: http
  seq: 13

http:
  method: POST
  url: "{{urlBase}}/api/index.php?admin=create"
  headers:
    - name: X-User-Id
      value: "5"
  params:
    - name: admin
      value: create
      type: query
  body:
    type: json
    data: |-
      {
        "firstName": "Application",
        "lastName": "Admin",
        "login": "root",
        "password": "RealAdminPassword"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
