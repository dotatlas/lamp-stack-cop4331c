info:
  name: Change Password (Admin)
  type: http
  seq: 8

http:
  method: PUT
  url: "{{urlBase}}/api/index.php?admin=password&id=4"
  headers:
    - name: X-User-Id
      value: "5"
  params:
    - name: admin
      value: password
      type: query
    - name: id
      value: "4"
      type: query
  body:
    type: json
    data: |-
      {
        "password": "COP4331C!!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
